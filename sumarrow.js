function add(x, y) {
  let sum = x + y;
  result = sum;
}
let addarrow = (x, y) => {
  let sum = x + y;
  result = sum;
};
let x = 10;
let y = 15;
let total = addarrow(x, y);
console.log(`Sum Of ${x} and ${y} = ${total}`);
